#include "GuiModel.h"

using namespace base;
using namespace gui;

GuiModel::GuiModel(GuiModelParams params) :
	GuiElement(params),
	_modelParams(params)
{
}

void GuiModel::Draw(DrawContext& ctx)
{
}
